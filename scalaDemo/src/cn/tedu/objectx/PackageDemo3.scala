package cn.tedu.objectx

// import java.util.{Arrays, Scanner}

// _表示通配符，导入一个包下的所有的类但是不包括子包下的类
import java.util._

object PackageDemo3 {

  def main(args: Array[String]): Unit = {
    // import java.util.Scanner

    var s = new Scanner(System.in)
    var arr = Array[Int](3, 5, 8, 9)
    Arrays.sort(arr)

  }

}
